// Initialize Bootstrap Carousel with options
$(document).ready(function () {
    $('#heroCarousel').carousel({
        interval: 3000, // Slide duration (in ms)
        ride: 'carousel' // Automatic sliding
    });
});
